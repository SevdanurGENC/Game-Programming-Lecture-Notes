using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bilesenler : MonoBehaviour
{
    Transform hareket;
    void Start()
    {
        //hareket = gameObject.GetComponent<Transform>(); 
        //hareket = transform;
    }

    // Update is called once per frame
    void Update()
    {
        //hareket.Rotate(10,0,0); 
        //hareket.Translate(1, 0, 0);
    }
}
